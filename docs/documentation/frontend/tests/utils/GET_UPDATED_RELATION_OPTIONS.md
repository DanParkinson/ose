## Navigation

[← Back to TESTING_OVERVIEW.md](/docs/documentation/frontend/tests/TESTING_OVERVIEW.md)

---

## Single Relation Updates

| Test Name | Purpose |
|---|---|
| single relation fields return the selected option ID | Verifies single relation fields return the selected option identifier directly. |

## Multiple Relation Updates

| Test Name | Purpose |
|---|---|
| multiple relation fields add unselected option IDs | Verifies unselected option IDs are added to multiple relation values. |
| multiple relation fields remove selected option IDs | Verifies selected option IDs are removed when toggled again. |
| multiple relation fields preserve existing selected values | Verifies existing selected relation values remain when adding new options. |
| empty multiple relation values initialise correctly | Verifies empty multiple relation values initialise correctly when the first option is selected. |

## optionValue Handling

| Test Name | Purpose |
|---|---|
| custom optionValue keys are supported | Verifies custom option identifier keys are used correctly during updates. |
