## Navigation

[← Back to TESTING_OVERVIEW.md](/docs/documentation/frontend/tests/TESTING_OVERVIEW.md)

## LoginForm

| Test Name | Purpose |
|---|---|
| shows the initial login form | Verifies the login form, inputs, and navigation links render correctly. |
| updates email and password inputs when user types | Verifies the email and password inputs update correctly when the user types. |
| calls login with entered values and navigates home on success | Verifies successful login submits the correct credentials and redirects the user home. |
| displays errors when login fails | Verifies field and non-field validation errors are displayed when login fails. |
