## Navigation

[← Back to TESTING_OVERVIEW.md](/docs/documentation/frontend/tests/TESTING_OVERVIEW.md)

## DashboardFilterPanel

| Test Name | Purpose |
|---|---|
| renders filter panel heading | Verifies the main FILTER heading renders correctly. |
| renders all filter section titles | Verifies all filter group titles render correctly from the provided filter configuration. |
| passes correct filter data to FilterOptions | Verifies filter keys and active filter values are passed correctly to `FilterOptions`. |
| calls onFilterChange when a filter option is clicked | Verifies clicking a filter option triggers `onFilterChange` with the correct filter key and value. |
